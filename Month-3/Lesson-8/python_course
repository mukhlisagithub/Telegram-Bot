1) Foundation
1) Foundation

2) Python Core
2) Python Core

3) Files & OOP
3) Files & OOP

4) Database
4) Database

5) Django - 1
5) python Advanced

6) Django - 2 + Leetcode, Linkedln, Algo DS
6) Django - 1

7) ----------
7) Django - 2

8) ----------
8) Django - 3 + Leetcode, Linkedln, Algo DS





# def sortString(s):
#     # Initialize an empty result string
#     result = ""
#
#     # Create a frequency dictionary to store the count of each character in the input string
#     freq = {}
#
#     # Count the frequency of each character in the input string
#     for char in s:
#         freq[char] = freq.get(char, 0) + 1
#
#     # Sort the unique characters in ascending order
#     chars = sorted(set(s))
#
#     # Repeat the steps until all characters are picked from s
#     while freq:
#         # Append the smallest character to the result
#         for char in chars:
#             if char in freq:
#                 result += char
#                 freq[char] -= 1
#
#                 # Remove the character from the frequency dictionary if its count becomes zero
#                 if freq[char] == 0:
#                     del freq[char]
#
#         # Append the largest character to the result
#         for char in reversed(chars):
#             if char in freq:
#                 result += char
#                 freq[char] -= 1
#
#                 # Remove the character from the frequency dictionary if its count becomes zero
#                 if freq[char] == 0:
#                     del freq[char]
#
#     return result

s = "aaabbbccc"
print(sortString(s))

